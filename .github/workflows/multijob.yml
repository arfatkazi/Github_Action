name: MULTI JOB CI


on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    name: lint section
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: echo "Run lint here"

  test:
    name: test section
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: echo "test here"

  build:
    name: build section
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: echo "build app here"


      

   

        
