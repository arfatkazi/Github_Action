name: Python-app

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
 

jobs:
  test:
    name: python-app
    runs-on: ubuntu-latest
    steps:
    - name: checkout code
      uses: actions/checkout@v5
    - name: Setup python
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade zip setuptools wheel
        pip install -r requirements.txt

    - name: Run tests
      run: pytest
    
